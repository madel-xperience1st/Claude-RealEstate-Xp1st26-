<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:salesforce="http://www.mulesoft.org/schema/mule/salesforce"
      xmlns:apikit="http://www.mulesoft.org/schema/mule/mule-apikit"
      xmlns:secure-properties="http://www.mulesoft.org/schema/mule/secure-properties"
      xmlns:os="http://www.mulesoft.org/schema/mule/os"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/salesforce http://www.mulesoft.org/schema/mule/salesforce/current/mule-salesforce.xsd
        http://www.mulesoft.org/schema/mule/mule-apikit http://www.mulesoft.org/schema/mule/mule-apikit/current/mule-apikit.xsd
        http://www.mulesoft.org/schema/mule/secure-properties http://www.mulesoft.org/schema/mule/secure-properties/current/mule-secure-properties.xsd
        http://www.mulesoft.org/schema/mule/os http://www.mulesoft.org/schema/mule/os/current/mule-os.xsd">

    <!-- ============================================ -->
    <!-- CONFIGURATION PROPERTIES                      -->
    <!-- ============================================ -->
    <configuration-properties file="application-${mule.env}.yaml" />

    <!-- ============================================ -->
    <!-- HTTP LISTENER (Inbound - iOS App connects)    -->
    <!-- ============================================ -->
    <http:listener-config name="HTTP_Listener_Config" doc:name="HTTP Listener Config">
        <http:listener-connection
            host="0.0.0.0"
            port="${http.port}" />
    </http:listener-config>

    <!-- ============================================ -->
    <!-- APIKIT ROUTER CONFIG                          -->
    <!-- ============================================ -->
    <apikit:config name="PropHub_API_Config"
                   api="api/prophub-api.raml"
                   raml="api/prophub-api.raml"
                   outboundHeadersMapName="outboundHeaders"
                   httpStatusVarName="httpStatus" />

    <!-- ============================================ -->
    <!-- SALESFORCE CONNECTOR                          -->
    <!-- Supports both Username-Password (trial) and  -->
    <!-- OAuth JWT Bearer (production)                 -->
    <!-- ============================================ -->
    <salesforce:sfdc-config name="Salesforce_Config" doc:name="Salesforce Config">
        <salesforce:basic-connection
            username="${sf.username}"
            password="${sf.password}"
            securityToken="${sf.security.token}"
            url="${sf.login.url}" />
    </salesforce:sfdc-config>

    <!-- ============================================ -->
    <!-- OBJECT STORE (JWT token cache, sessions)      -->
    <!-- ============================================ -->
    <os:object-store name="Token_Store"
                     persistent="true"
                     entryTtl="3600"
                     entryTtlUnit="SECONDS"
                     maxEntries="1000" />

    <os:object-store name="Chat_Session_Store"
                     persistent="false"
                     entryTtl="1800"
                     entryTtlUnit="SECONDS"
                     maxEntries="100" />

    <!-- ============================================ -->
    <!-- GLOBAL VARIABLES                              -->
    <!-- ============================================ -->
    <global-property name="api.version" value="v1" />

</mule>
